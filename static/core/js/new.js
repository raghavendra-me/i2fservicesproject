// Function to show preloader
function showPreloader() {
        $(".optech-preloader-wrap").fadeIn(500);  // Adjust timing if necessary
      }
      
      // Function to hide preloader
      
function hidePreloader() {
        $(".optech-preloader-wrap").fadeOut(500);  // Adjust timing if necessary
      }